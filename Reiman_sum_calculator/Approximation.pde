class Approximation { // class for calculating and drawing different types of Reimann Sums
  
  private Graph graph;
  private int n; // number of sub intervals
  private float dx; // width of each sub rectangle
  Approximation (Graph inputGraph, int subIntervalSize) {
    graph = inputGraph;
    n = subIntervalSize;
  }
  
  
  
}